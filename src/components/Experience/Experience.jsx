import React from "react";
import "./Experience.css";

const Experience = () => {
  // Placeholder section — kept in the layout so it can be filled in later
  // without restructuring App.jsx. Renders nothing visible right now.
  return <section id="experience" aria-hidden="true" />;
};

export default Experience;
